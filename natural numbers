#include<stdio.h>
int main()
{
	int n,i,sum=0;
	printf("enter the value of n:");
	scanf("%d",&n);
	printf("enter the n numbers are:\n");
	for(i=0;i<=n;i++)
	{
		printf("\t%d",i);
		sum=sum+i;
	}
	printf("\n\nsum of first n naturals numbers is:%d",sum);
	return 0;
}
